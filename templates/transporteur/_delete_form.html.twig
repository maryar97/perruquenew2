<form method="post" action="{{ path('app_transporteur_delete', {'id': transporteur.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transporteur.id) }}">
    <button class="btn">Supprimer</button>
</form>
