<form method="post" action="{{ path('app_produit_crud_supprimer', {'id': produit.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir suprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('supprimer' ~ produit.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
